---
 - name : Create user
   hosts: webservers
   become: yes

   vars_files:
   - vars/users.yml

   tasks:
   - name: Create user
     user:
      name: "{{ user }}"
      group: "{{ group }}"
      home: "{{ homedir }}"
      shell: "{{ shell }}" 
